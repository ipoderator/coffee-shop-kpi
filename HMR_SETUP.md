# Hot Module Replacement (HMR) - Автоматическая перезагрузка

## Что настроено

Проект настроен для автоматической перезагрузки изменений без ручного перезапуска сервера.

### Технологии:
- **Vite HMR** - мгновенное обновление изменений в браузере
- **React Fast Refresh** - сохранение состояния компонентов при обновлении
- **WebSocket** - соединение для передачи обновлений
- **tsx --watch** - автоматическая перезагрузка backend при изменениях

## Как это работает

1. **Изменения в компонентах React** (`client/src/**/*.tsx`) - автоматически применяются без перезагрузки страницы
2. **Изменения в стилях** (`client/src/**/*.css`) - применяются мгновенно
3. **Изменения в shared файлах** (`shared/**/*.ts`) - триггерят перезагрузку зависимых модулей
4. **Изменения в backend** (`server/**/*.ts`) - автоматически перезагружают сервер через `tsx --watch`

## Запуск в режиме разработки

```bash
npm run dev
```

Сервер автоматически запустится с поддержкой HMR на порту 5000.

## Что происходит при изменении файлов

### ✅ Автоматически обновляется (без перезагрузки страницы):
- Компоненты React (`.tsx`, `.ts`)
- Стили CSS
- Shared типы и схемы
- Конфигурация Vite

### ✅ Автоматически перезагружает сервер:
- Backend код (`server/**/*.ts`) - сервер автоматически перезапускается через `tsx --watch`
- Конфигурация Express (`server/index.ts`)
- Маршруты API (`server/routes/**/*.ts`)
- Middleware (`server/utils/**/*.ts`)
- Утилиты и аналитика (`server/utils/**/*.ts`)

## Проверка работы HMR

### Frontend (React):
1. Откройте браузер и откройте DevTools (F12)
2. В консоли должны появиться сообщения:
   ```
   [HMR] Обновление модулей: /src/pages/ProfitabilityPage.tsx
   [HMR] Модули обновлены успешно
   ```
3. Измените любой компонент в `client/src/` и сохраните файл
4. Изменения должны появиться в браузере мгновенно без перезагрузки страницы

### Backend (Express):
1. Запустите сервер: `npm run dev`
2. В консоли терминала вы увидите:
   ```
   🚀 Starting Coffee KPI server...
   ✅ Plugins registered successfully
   ✅ Routes registered successfully
   ```
3. Измените любой файл в `server/` (например, добавьте console.log в `server/index.ts`)
4. Сохраните файл
5. В консоли терминала вы увидите автоматический перезапуск:
   ```
   [tsx] watching for changes...
   [tsx] reloading...
   🚀 Starting Coffee KPI server...
   ```
6. Сервер перезапустится автоматически без ручного вмешательства

## Сохранение состояния

При использовании HMR состояние приложения сохраняется:
- ✅ Состояние React компонентов (если возможно)
- ✅ Локальное хранилище (localStorage)
- ✅ Сессии пользователя
- ✅ Данные в React Query кеше

## Решение проблем

### Frontend HMR не работает:
1. Проверьте, что сервер запущен в режиме разработки: `NODE_ENV=development`
2. Проверьте консоль браузера на наличие ошибок WebSocket
3. Убедитесь, что порт не заблокирован файрволом
4. Попробуйте перезагрузить страницу с очисткой кеша (Ctrl+Shift+R)

### Backend не перезагружается автоматически:
1. Убедитесь, что используется скрипт `npm run dev` (не `npm start`)
2. Проверьте, что установлен `tsx` версии 4.x или выше: `npm list tsx`
3. Проверьте консоль терминала на наличие сообщений `[tsx] watching for changes...`
4. Убедитесь, что файл сохранен (Ctrl+S)
5. Проверьте, что изменяемые файлы находятся в директории `server/` или `shared/`

### Изменения не применяются:
1. Проверьте консоль терминала на наличие ошибок компиляции
2. Убедитесь, что файл сохранен (Ctrl+S)
3. Проверьте, что файл находится в правильной директории (`client/src/` или `server/`)
4. Для backend: дождитесь перезапуска сервера (обычно 1-2 секунды)

### Ошибки компиляции:
- TypeScript ошибки блокируют HMR и автоматическую перезагрузку backend
- Исправьте ошибки в файле, и система снова заработает
- Проверьте логи в терминале для деталей ошибок
- Backend перезапустится автоматически после исправления ошибок

## Дополнительные настройки

Все настройки HMR находятся в:
- `vite.config.ts` - основная конфигурация Vite (frontend HMR)
- `server/vite.ts` - настройки интеграции Vite с Express
- `client/src/main.tsx` - обработчики HMR событий для frontend
- `package.json` - скрипт `dev` с флагом `--watch` для автоматической перезагрузки backend

## Примечания

- В режиме разработки HMR работает через WebSocket (ws://localhost:5000)
- В продакшене HMR отключен, используется статическая сборка
- Состояние авторизации сохраняется благодаря localStorage и сессиям
- Backend перезагружается полностью при изменении файлов (это нормально и занимает 1-2 секунды)
- `tsx --watch` автоматически отслеживает все импортированные файлы в `server/` и `shared/`
- При изменении shared файлов перезагружаются и frontend, и backend (если они импортируют эти файлы)

